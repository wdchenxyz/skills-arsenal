# Skills Arsenal

Personal agent skills repository.

Clone directly into `~/.agents/skills`:

```bash
git clone git@github.com:wdchenxyz/skills-arsenal.git "$HOME/.agents/skills"
```

Each top-level directory is a skill and may include `SKILL.md`, `references/`, `templates/`, `assets/`, and other skill-specific files.
